import { EmployeeAttDetailsData } from './employee-att-details-data';

describe('EmployeeAttDetailsData', () => {
  it('should create an instance', () => {
    expect(new EmployeeAttDetailsData()).toBeTruthy();
  });
});
