import { EmployeeQualification } from './employee-qualification';

describe('EmployeeQualification', () => {
  it('should create an instance', () => {
    expect(new EmployeeQualification()).toBeTruthy();
  });
});
