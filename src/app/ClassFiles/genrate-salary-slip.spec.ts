import { GenrateSalarySlip } from './genrate-salary-slip';

describe('GenrateSalarySlip', () => {
  it('should create an instance', () => {
    expect(new GenrateSalarySlip()).toBeTruthy();
  });
});
